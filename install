# Clone the repository
git clone https://github.com/dev-760/GalvaSim.git

# Change into the repository directory
cd GalvaSim

# Install the required dependencies
pip install -r requirements.txt

# Run the GalvaSim tool
python3 GalvaSim.py
